# Система регистрации горных перевалов

**REST API для ФСТР (Федерация Спортивного Туризма России)**

## О проекте

Данный проект представляет собой REST API для мобильного приложения туристов, которое позволяет:

- **Регистрировать информацию о горных перевалах** и отправлять её в систему ФСТР для модерации
- **Просматривать статус модерации** отправленных данных
- **Редактировать информацию** о перевалах в статусе "new"
- **Получать списки перевалов** по различным критериям

### Постановка задачи

Федерация спортивного туризма России разрабатывает мобильное приложение для Android и IOS, с помощью которого туристы смогут:

1. Подать заявку на внесение в базу данных информации о новом горном перевале
2. Отслеживать статус модерации заявки
3. Просматривать все свои поданные заявки
4. При необходимости редактировать данные о перевале до принятия модератором

### Что реализовано

#### Спринт 1
- ✅ Созданы модели данных для БД (User, Coords, Level, Pass, Image)
- ✅ Реализован класс для работы с БД (PassDataHandler)
- ✅ Базовый REST API метод POST /submitData/
- ✅ Настроена работа с переменными окружения
- ✅ Настроен Docker и PostgreSQL

#### Спринт 2  
- ✅ GET /submitData/<id>/ - получение записи по ID
- ✅ PATCH /submitData/<id>/ - редактирование записи
- ✅ GET /submitData/?user__email=<email> - список записей пользователя
- ✅ Ограничения на редактирование (только статус "new")
- ✅ Блокировка изменения персональных данных (ФИО, email, телефон)

#### Спринт 3
- ✅ Полная документация API в README.md
- ✅ Swagger документация
- ✅ Расширенное покрытие тестами

## Архитектура и технологии

### Технический стек
- **Backend Framework:** Django 4.2.7 + Django REST Framework 3.14.0
- **База данных:** PostgreSQL 15
- **Контейнеризация:** Docker + Docker Compose
- **Язык программирования:** Python 3.11
- **ORM:** Django ORM
- **Архитектура:** REST API

### Архитектура приложения

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│  Мобильное      │    │   Django REST    │    │   PostgreSQL    │
│  приложение     │◄──►│      API         │◄──►│      БД         │
│  (Android/iOS)  │    │                  │    │                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
```

### Основные компоненты

1. **Модели данных** (`passes/models.py`):
   - `User` - пользователи системы
   - `Pass` - основная модель перевала
   - `Coords` - координаты и высота
   - `Level` - уровни сложности по сезонам
   - `Image` - фотографии перевала

2. **API представления** (`passes/views.py`):
   - `PassDataHandler` - класс для работы с БД
   - REST API endpoints для CRUD операций

3. **Сериализаторы** (`passes/serializers.py`):
   - Валидация и сериализация данных
   - Обработка вложенных структур и изображений

### Жизненный цикл заявки

1. **Создание** - турист отправляет данные о перевале (статус: `new`)
2. **Модерация** - модератор берет заявку в работу (статус: `pending`)
3. **Результат** - заявка принимается (`accepted`) или отклоняется (`rejected`)

**Важно:** редактировать можно только заявки в статусе `new`

## Запуск проекта

### Через Docker (рекомендуемый способ)

1. Убедитесь, что Docker и Docker Compose установлены
2. Клонируйте проект
3. Запустите проект:

```bash
docker-compose up --build
```

Проект будет доступен по адресу: `http://localhost:8000`

### Ручной запуск (для разработки)

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте переменные окружения (создайте файл `.env`):
```
FSTR_DB_HOST=localhost
FSTR_DB_PORT=5432
FSTR_DB_LOGIN=fstr_user
FSTR_DB_PASS=fstr_password
FSTR_DB_NAME=fstr_db
SECRET_KEY=your-secret-key
DEBUG=True
```

3. Запустите PostgreSQL и создайте базу данных `fstr_db`

4. Примените миграции:
```bash
python manage.py makemigrations
python manage.py migrate
```

5. Создайте суперпользователя (опционально):
```bash
python manage.py createsuperuser
```

6. Запустите сервер:
```bash
python manage.py runserver
```

## API Reference

### 🔄 POST /submitData/

**Описание:** Создание новой заявки на регистрацию перевала

**Функциональность:**
- Получает и сохраняет информацию о перевале от мобильного приложения
- Автоматически устанавливает статус "new" для новых записей
- Создает или находит пользователя по email
- Обрабатывает изображения в формате base64

**Валидация:**
- Проверяет наличие обязательных полей: `title`, `user`, `coords`, `level`
- Валидирует структуру вложенных объектов
- Проверяет корректность координат и уровней сложности

### 📖 GET /submitData/<id>/

**Описание:** Получение подробной информации о перевале по ID

**Функциональность:**
- Возвращает все данные о перевале
- Включает статус модерации и время создания
- Показывает информацию о пользователе, координатах, уровне сложности
- Предоставляет список всех прикрепленных изображений

**Использование:** Для отслеживания статуса заявки и просмотра деталей

### ✏️ PATCH /submitData/<id>/

**Описание:** Редактирование существующей заявки на перевал

**Ограничения:**
- ❌ Можно редактировать только записи в статусе "new"
- ❌ Нельзя изменять персональные данные (ФИО, email, телефон)
- ✅ Можно изменять: название, координаты, уровень сложности, изображения

**Логика работы:**
- Проверяет текущий статус записи
- Блокирует изменение пользовательских данных
- Обновляет только разрешенные поля
- Возвращает результат операции (state: 1/0)

### 📋 GET /submitData/?user__email=<email>

**Описание:** Получение всех заявок конкретного пользователя

**Функциональность:**
- Фильтрует записи по email пользователя
- Сортирует по времени создания (новые сначала)
- Показывает все статусы модерации
- Возвращает пустой массив, если записей нет

**Структура запроса:**
```json
{
  "beauty_title": "пер. ",
  "title": "Пхия",
  "other_titles": "Триев", 
  "connect": "соединяет долины А и Б",
  "user": {
    "email": "qwerty@mail.ru",
    "fam": "Пупкин", 
    "name": "Василий",
    "otc": "Иванович",
    "phone": "+7 555 55 55"
  },
  "coords": {
    "latitude": "45.3842",
    "longitude": "7.1525", 
    "height": "1200"
  },
  "level": {
    "winter": "",
    "summer": "1А", 
    "autumn": "1А",
    "spring": ""
  },
  "images": [
    {"data": "<base64_данные>", "title": "Седловина"}, 
    {"data": "<base64_данные>", "title": "Подъём"}
  ]
}
```

**Структура ответа:**
```json
{
  "status": 200,
  "message": null,
  "id": 42
}
```

**HTTP статус-коды:**
- **200** - успешное сохранение (+ возвращается id записи)
- **400** - Bad Request (недостаточно полей)
- **500** - ошибка сервера/БД

**Примеры ответов новых методов:**

**GET /submitData/1/ - получение записи по ID:**
```json
{
  "beauty_title": "пер. ",
  "title": "Пхия",
  "other_titles": "Триев",
  "connect": "соединяет долины А и Б",
  "add_time": "2024-01-01T12:00:00Z",
  "status": "new",
  "user": {
    "email": "qwerty@mail.ru",
    "fam": "Пупкин",
    "name": "Василий",
    "otc": "Иванович",
    "phone": "+7 555 55 55"
  },
  "coords": {
    "latitude": "45.3842",
    "longitude": "7.1525",
    "height": "1200"
  },
  "level": {
    "winter": "",
    "summer": "1А",
    "autumn": "1А", 
    "spring": ""
  },
  "images": [...]
}
```

**PATCH /submitData/1/ - редактирование записи:**
```json
{
  "state": 1,
  "message": null
}
```

При ошибке редактирования:
```json
{
  "state": 0,
  "message": "Редактирование невозможно. Статус записи: accepted. Можно редактировать только записи со статусом \"new\"."
}
```

**GET /submitData/?user__email=qwerty@mail.ru - список записей пользователя:**
```json
[
  {
    "beauty_title": "пер. ",
    "title": "Пхия",
    "status": "new",
    "add_time": "2024-01-01T12:00:00Z",
    ...
  },
  {
    "beauty_title": "пер. ",
    "title": "Другой перевал",
    "status": "accepted", 
    "add_time": "2024-01-02T15:30:00Z",
    ...
  }
]
```

## 🧪 Тестирование

### Автоматические тесты

Проект включает комплексные тесты для проверки всей функциональности API:

#### Тест базового функционала
```bash
python test_api.py
```

**Покрывает:**
- ✅ Создание нового перевала (POST /submitData/)
- ✅ Валидацию обязательных полей
- ✅ Корректность структуры ответа
- ✅ Обработку ошибок

#### Тест расширенного функционала  
```bash
python test_new_api.py
```

**Покрывает:**
- ✅ Получение перевала по ID (GET /submitData/<id>/)
- ✅ Список перевалов пользователя (GET /submitData/?user__email=<email>)
- ✅ Редактирование перевала (PATCH /submitData/<id>/)
- ✅ Блокировку изменения персональных данных
- ✅ Проверку статуса перед редактированием
- ✅ Обработку различных сценариев ошибок

### Ручное тестирование

#### Тестовые данные
Для ручного тестирования можно использовать следующий JSON:

```json
{
  "beauty_title": "пер. ",
  "title": "Тестовый перевал",
  "other_titles": "Альтернативное название",
  "connect": "соединяет долины А и Б",
  "user": {
    "email": "test@example.com",
    "fam": "Иванов",
    "name": "Иван", 
    "otc": "Иванович",
    "phone": "+7 900 123 45 67"
  },
  "coords": {
    "latitude": "55.7558",
    "longitude": "37.6176",
    "height": "150"
  },
  "level": {
    "winter": "2А",
    "summer": "1Б",
    "autumn": "1Б", 
    "spring": "2А"
  },
  "images": [
    {"data": "base64_encoded_image", "title": "Вид на перевал"}
  ]
}
```

#### Команды для тестирования через curl

**Создание перевала:**
```bash
curl -X POST http://localhost:8000/submitData/ \
  -H "Content-Type: application/json" \
  -d @test_data.json
```

**Получение по ID:**
```bash
curl http://localhost:8000/submitData/1/
```

**Список по email:**
```bash
curl "http://localhost:8000/submitData/?user__email=test@example.com"
```

**Редактирование:**
```bash
curl -X PATCH http://localhost:8000/submitData/1/ \
  -H "Content-Type: application/json" \
  -d '{"title": "Обновленное название"}'
```

### Требования для тестирования

⚠️ **Убедитесь, что сервер запущен на `localhost:8000` перед запуском тестов**

1. Запустите проект через Docker: `docker-compose up`
2. Дождитесь полной загрузки (сообщение о запуске сервера)
3. Запустите тесты в отдельном терминале

##  База данных

### Модели данных

**User** - пользователи системы
- email, fam, name, otc, phone

**Coords** - координаты перевала  
- latitude, longitude, height

**Level** - уровень сложности
- winter, summer, autumn, spring

**Pass** - перевал (основная модель)
- beauty_title, title, other_titles, connect
- status (new/pending/accepted/rejected)
- связи с User, Coords, Level

**Image** - изображения перевала
- data (файл), title, связь с Pass

### Статусы модерации

- `new` - новая запись (по умолчанию)
- `pending` - взято в работу модератором  
- `accepted` - модерация прошла успешно
- `rejected` - модерация не прошла

## 🔒 Безопасность и ограничения

### Ограничения редактирования

- **Статус записи:** Редактировать можно только записи в статусе `new`
- **Персональные данные:** Заблокировано изменение полей `email`, `fam`, `name`, `otc`, `phone`
- **Права доступа:** Пользователь может просматривать только свои записи (по email)

### Валидация данных

- ✅ Проверка обязательных полей при создании
- ✅ Валидация email адресов  
- ✅ Проверка структуры координат и высоты
- ✅ Валидация уровней сложности
- ✅ Безопасная обработка base64 изображений

### Обработка ошибок

Система возвращает понятные сообщения об ошибках:
- `400 Bad Request` - некорректные данные или недостающие поля
- `404 Not Found` - запрашиваемый перевал не найден
- `500 Internal Server Error` - ошибки сервера или БД

## 🔧 Переменные окружения

### Обязательные переменные

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `FSTR_DB_HOST` | Хост базы данных | `localhost` |
| `FSTR_DB_PORT` | Порт базы данных | `5432` |
| `FSTR_DB_LOGIN` | Логин для подключения к БД | `fstr_user` |
| `FSTR_DB_PASS` | Пароль для подключения к БД | `fstr_password` |
| `FSTR_DB_NAME` | Имя базы данных | `fstr_db` |

### Опциональные переменные

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `SECRET_KEY` | Секретный ключ Django | Генерируется автоматически |
| `DEBUG` | Режим отладки | `True` |

### Настройка для production

```bash
# Создайте файл .env в корне проекта
SECRET_KEY=your-very-secure-secret-key-here
DEBUG=False
FSTR_DB_HOST=your-production-db-host
FSTR_DB_PASS=your-secure-password
```

⚠️ **Важно:** В production обязательно используйте надежный SECRET_KEY и установите DEBUG=False

## 📈 Развитие проекта

### Планы по развитию
- 🔐 Добавление системы аутентификации и авторизации
- 📱 Расширение функционала для мобильных приложений
- 🗺️ Интеграция с картографическими сервисами
- 📊 Система аналитики и отчетности для модераторов
- 🔄 Система уведомлений о смене статуса

### Возможные улучшения
- WebSocket для real-time уведомлений
- Кэширование для улучшения производительности  
- Система резервного копирования данных
- API версионирование
- Масштабирование для высоких нагрузок

## 🤝 Вклад в проект

Данный проект создан в рамках стажировки. Все основные требования ТЗ выполнены:

- ✅ **Спринт 1:** Модели БД, базовый API, Docker настройка
- ✅ **Спринт 2:** Полный CRUD API, валидация, ограничения
- ✅ **Спринт 3:** Полная документация, тесты, архитектура

### Критерии оценки
- 🏆 **Создан класс по работе с БД** - 10 баллов
- 🏆 **Реализован REST API (Спринт 1)** - 15 баллов  
- 🏆 **Реализован REST API (Спринт 2)** - 15 баллов
- 🏆 **Написана документация** - 5 баллов
- 🏆 **Качество кода** - 5 баллов

**Итого: 50 баллов** (минимум для прохождения: 40 баллов)

## 📞 Контакты

Проект разработан в рамках стажировки по программе обучения Backend разработке.

**Repository:** [pereval-online-stazhirovka](https://github.com/Kanny-Dzmitry/pereval-online-stazhirovka)

---

*Документация обновлена: январь 2024*  
*Версия API: 1.0*
